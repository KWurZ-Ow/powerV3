export default function handleOrdre(color:string, piece:string, start:string, finish:String){
    if(piece.charAt(0) === "E"){
        //type d'ordre = échange de pièces
        
    }else{
        //type d'ordre = déplacement de pièces

    }
}